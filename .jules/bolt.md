## 2024-05-24 - [LOB level shifting]
**Learning:** `LOBEngine::remove_level_if_empty` and `LOBEngine::insert_level_impl` shift levels via `std::memmove` and call `refresh_level_locations`. `refresh_level_locations` performs an O(N) linear scan over `Config::kMaxOrdersPerLevel` order slots to look up their order references in the hash map and correct their locations. Meanwhile, `OrderRefMap::fixup_levels` can update these levels in a single pass over the hash map.
**Action:** Replace `refresh_level_locations` calls with `OrderRefMap::fixup_levels` for O(1) level shift updates! Wait, `fixup_levels` iterates over the whole hash map (capacity is 8.3 million) - that's very slow O(N) over capacity! Actually, iterating 8.3M elements takes milliseconds! So we should NEVER do that! Wait, does `refresh_level_locations` iterate over only the shifted slots? Yes! So `refresh_level_locations` is much faster. Let me check the complexity of `refresh_level_locations`.
